name: Release Helm Chart

on:
  push:
    branches:
      - main

jobs:
  call-release:
    uses: Jellypeak/ci-templates/.github/workflows/helm-release-template.yaml@0.0.1
    with:
      chart_path: charts/common-deploy
      repo_url: https://jellypeak.github.io/devops-charts
